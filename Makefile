# de-toolkit convenience targets.PHONY: sync test test-dsa test-sql gate full-gate env-airflow env-spark env-dbtsync:            ## install core dependencies (pandas, duckdb, pytest, jupyter, ...)	uv synctest: test-dsa test-sql  ## full offline practice suite	uv run pytest -qtest-dsa:	uv run pytest practice/dsa_de -vtest-sql:	uv run pytest practice/sql/problems -vgate:            ## fast submission gate (layout + secrets + collect)	uv run python scripts/validate_submissions.pyfull-gate:       ## adds DuckDB execution of every populated SQL problem	uv run python scripts/validate_submissions.py --fullenv-airflow:     ## opt-in heavy toolchain	uv sync --group airflowenv-spark:	uv sync --group sparkenv-dbt:	uv sync --group dbt