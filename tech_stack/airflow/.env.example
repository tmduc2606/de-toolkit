AIRFLOW_UID = 50000
# Required before `docker compose up` — generate and paste (keep .env local):
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# FERNET_KEY = <paste generated key here>