# Colab Tips & Tricks

Practical Google Colab + Bash workflow notebook — shortcuts, shell magic, and
environment tricks useful when running shared learning notebooks.

> Sibling modules with a different purpose: `dsa_de/` (LeetCode-style problems
> with tests), `data_cleaning/` (pandas wrangling chapters).

| Notebook | Topic |
|---|---|
| `1.1. Tricks_in_colab_and_Bash-Tutorial.ipynb` | Colab shortcuts, `!`/`%` shell magic, file handling |

## Run

Open in Google Colab, or locally:

```bash
uv sync
uv run jupyter lab
```
