def data_transformations(data):
    """Apply list comprehension-based transformations to a dataset.

    DE reframing: apply functional transformations (filter, map, aggregate) to a dataset using list comprehensions.

    Demonstrates functional programming patterns for data
    transformation: filtering, mapping, and aggregating
    using Python list comprehensions and generator expressions.

    In a data engineering context, this applies common
    transformations to tabular data (e.g., filtering rows,
    projecting columns, computing derived fields).

    Args:
        data: A list of dictionaries representing records.

    Returns:
        A transformed list of records.
    """
    raise NotImplementedError("This problem has not yet been solved. "
                              "DE reframing: apply functional transformations (filter, map, aggregate) to a dataset using list comprehensions.")