# Big Data Analytics

Practice notebooks for big-data analytics concepts — volume/velocity/veracity,
batch vs. stream thinking, and aggregations that outgrow a single machine.

> Sibling modules with a different purpose: `dsa_de/` (LeetCode-style problems
> with tests), `data_cleaning/` (pandas wrangling chapters). This folder is
> concept learning — read, run, adapt.

**Status:** placeholder — new practice notebooks land here following the
notebook-module convention (a README table listing each notebook + topic, as in
`python-computing/`).

## Run

```bash
uv sync
uv run jupyter lab
```
