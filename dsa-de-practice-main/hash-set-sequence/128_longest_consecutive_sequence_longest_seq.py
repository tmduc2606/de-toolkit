def longest_consecutive_sequence(nums):
    """Find the length of the longest consecutive sequence in an unsorted dataset.

    DE reframing: find the longest consecutive run of record IDs in a dataset.

    In a data engineering context, this identifies the longest
    run of consecutive record IDs or timestamps in a dataset,
    useful for detecting continuous data coverage or gaps.

    Args:
        nums: A list of integers representing record IDs or timestamps.

    Returns:
        The length of the longest consecutive sequence.
    """
    raise NotImplementedError("This problem has not yet been solved. "
                              "DE reframing: find the longest consecutive run of record IDs in a dataset.")